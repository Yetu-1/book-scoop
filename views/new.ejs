<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="styles/main.css">
    <title><%= locals.heading%></title>
</head>
<body>
    <div class="container">
        <h1><%= locals.heading%></h1>
        <% if (locals.book) { %>
            <form id="editForm" method="post" action="/update">
            <input name="book_id" type="hidden" value="<%= book.id%>">
            <input type="text" name="title" value="<%= book.title %>">
            <textarea id="content" name="review" rows="10"><%=book.review %></textarea>
            <input type="text" name="rating" value="<%= book.rating %>">
            <button class="full-width" type="submit">
                <%= submit %>
            </button>
            </form>
            <% } else {%>
            <form id="newPostForm" method="post" action="/add">
                <input type="text" name="title" placeholder="Title" required>
                <textarea name="review" placeholder="review" required rows="10"></textarea>
                <input type="text" name="rating" placeholder="rating" required>
                <button class="full-width" type="submit">
                <%= submit %>
                </button>
            </form>
        <% } %>
    </div>
</body>
</html>